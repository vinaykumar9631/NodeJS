<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>airbnb</title> -->
<%- include('partials/head') %>
</head>
<body>

<%- include('partials/nav') %>
    <div class="mt-6 flex flex-col items-center justify-center">
      <div class="w-2/5 max-w-sm bg-gray-200 sm-auto p-5 text-center rounded-lg shadow-2xl">
         <h2 class="text-3xl font-extrabold text-gray-800">
          Here are our Registered Homes:
        </h2>
      </div>

      <!-- <div class="mt-6 bg-white p-6 rounded-lg shadow-2xl w-2/5">
        <h1 class="text-4xl font-extrabold text-red-700">House name List :</h1>
        <ul class="mt-4">
          <% registeredHomes.forEach(home => { %>
            <li class="text-xl font-medium text-blue-700 mt-4">
              <%= home.HouseName%>
            </li>
          <% }) %>
        </ul>
      </div> -->

      <div class=" grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">

        <% registeredHomes.forEach(home => { %>
          <div class="w-96 bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-2xl transition-all duration-300 border border-gray-200">

                  <!-- Image -->
                  <img 
                    src="<%= home.photoUrl %>" 
                    alt="<%= home.Image  %>"
                    class="w-full h-56 object-cover"
                  />

                  <!-- Card Body -->
              <div class="p-5">

                <!-- House Name -->
                <h2 class="text-2xl font-semibold text-gray-900">
                  <%= home.HouseName %>
                </h2>

                <!-- Location -->
                <p class="text-gray-600 text-sm mt-1">
                  📍 <%= home.location %>
                </p>

                <!-- Price -->
                <p class="text-red-700 font-semibold text-lg mt-3">
                  <span class="text-blue-700 font-semibold">₹</span> <%= home.price %> <span class="text-blue-700 font-semibold">/ night</span>
                </p>

                    <!-- Rating -->
                <p class="text-yellow-500 font-medium mt-2">
                   ⭐ <%= home.rating %> / 10
                </p>

                <!-- Button -->
                <button class="mt-4 w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-xl font-semibold transition">
                   View Details
                </button>

            </div>
          </div>
        <% }) %>

      </div>



    </div>
</body>
</html>
