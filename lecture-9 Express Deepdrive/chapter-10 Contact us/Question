
Create a new project 

1. Install nodemon and express.
2. Add two dummy middlewere that logs request path and request method respectively.
3. Add a third middlewere that return a responce.
4. Now add handling using two more middlewere that handle path "/", a request to "/contact-us" page.
5. Contact us should return a form with name and email as input fields that submits to "contact-us"
   page also.
6. Also handle POST incoming request to "contact-us" path using a separate middlewere.